📱 OpenWebUI-Telegram-Bot

A simple bot script that connects the OpenWebUI API with a Telegram Bot.
Using an API key generated in OpenWebUI, this tool allows you to interact with AI models directly through Telegram chat 🧠🤖

📦 Features

✔ Seamless integration between OpenWebUI API and Telegram Bot
✔ Simple installation and execution script
✔ Can be run directly on the server where the OpenWebUI instance is running

📥 Prerequisites
1. Create a Telegram Bot Token

Open Telegram and search for @BotFather

Enter /newbot and set the bot name and username

Copy the generated Bot Token

2. Generate an OpenWebUI API Key

Access the OpenWebUI web interface

Go to Settings → API Keys

Click “Create New Key” and copy the API key

3. (Optional) Check Docker Network Name

If you are using Docker, identify the Docker network where OpenWebUI is running:

docker network ls


→ Note the network name used by the OpenWebUI container

🚀 Installation & Execution
1. Download the script
wget https://raw.githubusercontent.com/hackyhappy-labs/openwebui-telegram-bot-/main/start-openwebui-telegram-bot.sh

2. Grant execute permission
chmod +x start-openwebui-telegram-bot.sh

3. Run the script
./start-openwebui-telegram-bot.sh

Or run in a single command
curl -fsSL https://raw.githubusercontent.com/hackyhappy-labs/openwebui-telegram-bot-/main/start-openwebui-telegram-bot.sh | bash

⚙️ Configuration

When running the bot, you need to provide the following values either via prompts or environment variables:

Variable	Description
TELEGRAM_BOT_TOKEN	Telegram Bot token generated by BotFather
OPENWEBUI_API_KEY	API key generated in OpenWebUI
OPENWEBUI_API_URL	OpenWebUI server URL and port (e.g. http://localhost:3000/api)
Setting environment variables
export TELEGRAM_BOT_TOKEN="YOUR_BOT_TOKEN"
export OPENWEBUI_API_KEY="YOUR_API_KEY"
export OPENWEBUI_API_URL="http://your-server:port/api"

📌 Usage Example

Send a message to the bot on Telegram.
The bot forwards the message to the OpenWebUI API, processes it with the AI model, and sends the response back to Telegram.

User → Telegram Bot: Hello!
Bot → OpenWebUI API → AI Response → Telegram
